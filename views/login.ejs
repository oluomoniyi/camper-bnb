<%include ./partials/header %>

    <div class="section">
        <div class="container">
            <div class="row">
                <%include ./partials/sidebar %>
                <div class="title-area column col-lg-6 col-sm-12 col">
                    <h2>Login</h2>
                    <div class="separator separator-danger">✻</div>
                    <form action="/login" method="POST" style="">
                        <div class="form-group">
                            <label for="search">Username</label>
                            <input type="text" class="form-control" placeholder="username" name="username" id="username">
                            <label for="search">Password</label>
                            <input type="password" class="form-control" placeholder="password" name="password" id="password">
                            <button class="btn btn-primary btn-large button-get-started" style="display:inline-block" value="login">Login</button>
                        </div>
                        <!--button type="submit" class="btn btn-default">Submit</button document.getElementById('search').value(search) -->
                    </form>
                </div>
            </div>
        </div>
    </div>

<% include ./partials/footer %>